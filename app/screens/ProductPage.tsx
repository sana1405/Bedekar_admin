"use client";

import { useMemo } from "react";
import type { ReactNode } from "react";

type Entity = { _id?: string; id?: string; name?: string; category?: string | { _id?: string; name?: string } };
type Product = { _id?: string; id?: string; name: string; price?: number; category?: Entity | string; subCategory?: Entity | string; purity?: string; netWeight?: number; displayImage?: string; createdAt?: string | Date };
const money = (value?: number) => new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 }).format(value ?? 0);
const idOf = (item?: { _id?: string; id?: string } | string) => typeof item === "string" ? item : item?._id ?? item?.id ?? "";
const labelOf = (item?: Entity | string) => typeof item === "string" ? item : item?.name ?? "Unassigned";
const PRICE_RANGES = [{ value: "0-5k", label: "Rs 0 - Rs 5k", min: 0, max: 5000 }, { value: "5k-10k", label: "Rs 5k - Rs 10k", min: 5000, max: 10000 }, { value: "10k-25k", label: "Rs 10k - Rs 25k", min: 10000, max: 25000 }, { value: "25k-50k", label: "Rs 25k - Rs 50k", min: 25000, max: 50000 }, { value: "50k-100k", label: "Rs 50k - Rs 100k", min: 50000, max: 100000 }, { value: "100k+", label: "Rs 100k+", min: 100000, max: Infinity }];

type Props = { products: Product[]; categories?: Entity[]; subcategories?: Entity[]; search: string; setSearch: (value: string) => void; categoryFilter: string; setCategoryFilter: (value: string) => void; subcategoryFilter: string; setSubcategoryFilter: (value: string) => void; dateSort: string; setDateSort: (value: string) => void; priceRange: string; setPriceRange: (value: string) => void; purityFilter: string; setPurityFilter: (value: string) => void; onAdd: () => void; onView: (product: Product) => void; onEdit: (product: Product) => void; onDelete: (product: Product) => void };

export default function ProductPage({ products, categories = [], subcategories = [], search, setSearch, categoryFilter, setCategoryFilter, subcategoryFilter, setSubcategoryFilter, dateSort, setDateSort, priceRange, setPriceRange, purityFilter, setPurityFilter, onAdd, onView, onEdit, onDelete }: Props) {
  const uniquePurities = useMemo(() => Array.from(new Set(products.map(product => product.purity).filter((purity): purity is string => Boolean(purity)))).sort(), [products]);
  const displayedProducts = useMemo(() => { const query = search.trim().toLowerCase(); const range = PRICE_RANGES.find(item => item.value === priceRange); const min = range?.min ?? 0; const max = range?.max ?? Infinity; const result = products.filter(product => (!query || [product.name, labelOf(product.category), labelOf(product.subCategory)].some(value => value.toLowerCase().includes(query))) && (!categoryFilter || idOf(product.category) === categoryFilter) && (!subcategoryFilter || idOf(product.subCategory) === subcategoryFilter) && (product.price ?? 0) >= min && (product.price ?? 0) <= max && (!purityFilter || product.purity === purityFilter)); return dateSort === "newest" ? result.sort((a, b) => new Date(b.createdAt || 0).getTime() - new Date(a.createdAt || 0).getTime()) : dateSort === "oldest" ? result.sort((a, b) => new Date(a.createdAt || 0).getTime() - new Date(b.createdAt || 0).getTime()) : result; }, [products, search, categoryFilter, subcategoryFilter, priceRange, purityFilter, dateSort]);
  return <PageTop title="Product catalogue" text="Create, edit and curate every piece in your storefront." action="Add product" onAction={onAdd}><div className="product-filters card"><div className="toolbar-field toolbar-search"><label htmlFor="product-search">Search</label><input id="product-search" type="search" value={search} onChange={event => setSearch(event.target.value)} placeholder="Search products or categories" /></div><div className="toolbar-field toolbar-select"><label htmlFor="product-category">Category</label><select id="product-category" value={categoryFilter} onChange={event => setCategoryFilter(event.target.value)}><option value="">All categories</option>{categories.map(category => <option key={idOf(category)} value={idOf(category)}>{labelOf(category)}</option>)}</select></div>{subcategories.length > 0 && <div className="toolbar-field toolbar-select"><label htmlFor="product-subcategory">Subcategory</label><select id="product-subcategory" value={subcategoryFilter} onChange={event => setSubcategoryFilter(event.target.value)}><option value="">All subcategories</option>{subcategories.map(subcategory => <option key={idOf(subcategory)} value={idOf(subcategory)}>{labelOf(subcategory)}</option>)}</select></div>}<div className="toolbar-field toolbar-select"><label htmlFor="product-price">Price range</label><select id="product-price" value={priceRange} onChange={event => setPriceRange(event.target.value)}><option value="">All prices</option>{PRICE_RANGES.map(range => <option key={range.value} value={range.value}>{range.label}</option>)}</select></div>{uniquePurities.length > 0 && <div className="toolbar-field toolbar-select"><label htmlFor="product-purity">Purity</label><select id="product-purity" value={purityFilter} onChange={event => setPurityFilter(event.target.value)}><option value="">All purities</option>{uniquePurities.map(purity => <option key={purity} value={purity}>{purity}</option>)}</select></div>}<div className="toolbar-field toolbar-select"><label htmlFor="product-date">Sort by date</label><select id="product-date" value={dateSort} onChange={event => setDateSort(event.target.value)}><option value="">No sort</option><option value="newest">Newest first</option><option value="oldest">Oldest first</option></select></div>{(search || categoryFilter || subcategoryFilter || priceRange || purityFilter || dateSort) && <button className="secondary-button compact" type="button" onClick={() => { setSearch(""); setCategoryFilter(""); setSubcategoryFilter(""); setPriceRange(""); setPurityFilter(""); setDateSort(""); }}>Reset</button>}<div className="product-count">{displayedProducts.length} product{displayedProducts.length !== 1 ? "s" : ""}</div></div><section className="card data-table products-table"><div className="thead"><span>PRODUCT</span><span>CATEGORY</span><span>PRICE</span><span>DETAILS</span><span /></div>{displayedProducts.length ? displayedProducts.map(product => <div className="trow" key={idOf(product)}><ProductLine product={product} /><span className="tag">{labelOf(product.category)}</span><b>{money(product.price)}</b><span>{product.purity || "-"} · {product.netWeight || "-"}g</span><span className="actions"><button className="icon-button" title="View product" onClick={() => onView(product)}>View</button><button onClick={() => onEdit(product)}>Edit</button><button onClick={() => onDelete(product)}>Delete</button></span></div>) : <Empty label="No products match your search." />}</section></PageTop>;
}
function ProductLine({ product }: { product: Product }) { return <div className="product-name"><div className="thumbnail">{product.displayImage ? <img src={product.displayImage} alt="" /> : <span>✦</span>}</div><span><b>{product.name}</b><small>{labelOf(product.category)} · {labelOf(product.subCategory)}</small></span></div>; }
function PageTop({ title, text, action, onAction, children }: { title: string; text: string; action?: string; onAction?: () => void; children: ReactNode }) { return <><div className="welcome page-title"><div><p className="eyebrow">STORE MANAGEMENT</p><h1>{title}</h1><p className="muted">{text}</p></div>{action && <button className="gold" onClick={onAction}>{action}</button>}</div>{children}</>; }
function Empty({ label }: { label: string }) { return <div className="empty">◇<p>{label}</p></div>; }
